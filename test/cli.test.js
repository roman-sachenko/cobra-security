import { test } from 'node:test';
import assert from 'node:assert';

test('CLI basic structure', async (t) => {
  await t.test('should have scan command', () => {
    // Placeholder test - will be expanded
    assert.ok(true);
  });
});
