# pnpm configuration
# Security: Exact version pinning is enforced in package.json
# Production builds should use --frozen-lockfile
auto-install-peers=true
strict-peer-dependencies=false
shamefully-hoist=false
# Security: Prevent automatic updates
save-exact=true
